name: my-run-unit-test of feature branches

on:
  push:
    branches: 
      'feature/*'


jobs:
  my-run-unit-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3.3.0

      - name: my-run-unit-test
        run: nvn test
  
